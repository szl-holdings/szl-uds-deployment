# Copyright 2026 SZL Holdings
# SPDX-License-Identifier: Apache-2.0
#
# GENERATED by mesh/_authpolicy_gen.py from the v0.4.0 authorization matrix.
# Istio AuthorizationPolicy: https://istio.io/latest/docs/reference/config/security/authorization-policy/
# Schema (action ALLOW/DENY, from.source.principals, to.operation): same URL.
# ALLOW-implies-default-deny semantics: https://istio.io/latest/docs/ops/best-practices/security/

---
apiVersion: security.istio.io/v1
kind: AuthorizationPolicy
metadata:
  name: allow-mesh-to-a11oy
  namespace: szl-a11oy
  labels:
    app.kubernetes.io/name: a11oy
    szl.io/component: mesh-authz
    szl.io/mesh-version: v0.4.0
spec:
  selector:
    matchLabels:
      app: a11oy
  action: ALLOW
  rules:
    - from:
        - source:
            principals:
              - "cluster.local/ns/szl-rosie/sa/rosie"  # rosie
              - "cluster.local/ns/szl-amaru/sa/amaru"  # amaru
              - "cluster.local/ns/szl-sentra/sa/sentra"  # sentra
              - "cluster.local/ns/szl-vessels/sa/vessels"  # vessels
      to:
        - operation:
            ports: ["8080", "7860", "8443"]
