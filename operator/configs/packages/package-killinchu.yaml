# UDS Package CR for szl-killinchu
# Doctrine v11 LOCKED 749/14/163
apiVersion: uds.dev/v1alpha1
kind: Package
metadata:
  name: szl-killinchu
  namespace: szl-killinchu
  annotations:
    szl.io/doctrine-version: "v11"
    szl.io/doctrine-pin: "749/14/163"
    szl.io/kernel-commit: "c7c0ba17"
    szl.io/slsa-level: "L1"
spec:
  network:
    expose:
      - host: killinchu
        service: szl-killinchu
        port: 8080
        gateway: tenant
        selector:
          app: szl-killinchu
    allow:
      - direction: Egress
        description: "Keycloak OIDC token endpoint"
        remoteNamespace: keycloak
        remoteSelector:
          app.kubernetes.io/name: keycloak
        port: 8443
        remoteProtocol: TLS
      - direction: Egress
        description: "Peat mesh QUIC CRDT sync"
        remoteNamespace: peat-system
        remoteSelector:
          app.kubernetes.io/name: peat-mesh
        port: 4001
        remoteProtocol: UDP
      - direction: Ingress
        description: "IntraNamespace sidecar and probes"
        remoteGenerated: IntraNamespace
  sso:
    - clientId: uds-szl-killinchu
      name: "SZL Killinchu"
      protocol: openid-connect
      redirectUris:
        - "https://killinchu.uds.dev/*"
      webOrigins:
        - "https://killinchu.uds.dev"
      standardFlowEnabled: true
      enableAuthserviceSelector:
        app: szl-killinchu
      groups:
        anyOf:
          - /szl-operators
      secretConfig:
        name: szl-killinchu-oidc-secret
        template: |
          OIDC_CLIENT_ID: "{{ .clientId }}"
          OIDC_CLIENT_SECRET: "{{ .secret }}"
          OIDC_ISSUER: "https://sso.uds.dev/realms/uds"
  monitor:
    - description: "szl-killinchu Prometheus metrics"
      portName: http-metrics
      targetPort: 9090
      selector:
        app: szl-killinchu
      path: /metrics
      kind: ServiceMonitor
