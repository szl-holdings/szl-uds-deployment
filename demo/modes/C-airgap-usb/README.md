# Mode C — air-gap USB (wrapper)
Wraps the real, existing payload at `warhacker/usb/` (build_usb_payload.sh,
fallback/offline-deploy.sh, signed vessels Zarf, offline scenarios with real
crypto). See `warhacker/usb/DAY_OF_RUNBOOK.md` Paths A/B/C.
Run: `./run.sh --usb /path/to/warhacker/usb [--platform linux-amd64] [--dry-run]`
