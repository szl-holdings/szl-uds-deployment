# Copyright 2026 SZL Holdings
# SPDX-License-Identifier: Apache-2.0
#
# Runtime dependencies for the SZL receipts server.
# Pinned to EXACT versions (==) so the image build can never silently pull a
# newer release. A hashed lockfile (pip --require-hashes) remains the fully
# reproducible end state and is the documented follow-up.

# Ed25519 signing (PhD Crypto Finding A2 fix).
cryptography==49.0.0

# Real OpenTelemetry spans exported via OTLP/gRPC (PhD Systems Scope 9 fix).
opentelemetry-sdk==1.43.0
opentelemetry-exporter-otlp-proto-grpc==1.43.0
