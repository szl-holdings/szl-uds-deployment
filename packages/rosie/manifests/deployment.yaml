# Copyright 2026 SZL Holdings
# SPDX-License-Identifier: Apache-2.0
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: rosie
  namespace: szl-rosie
  labels:
    app: rosie
    app.kubernetes.io/name: rosie
    szl.io/component: uds-workload
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rosie
  template:
    metadata:
      labels:
        app: rosie
        app.kubernetes.io/name: rosie
    spec:
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        runAsGroup: 65532
        fsGroup: 65532
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: rosie
          # Live, cosign-signed image. Zarf rewrites this to the in-cluster
          # registry reference at deploy time.
          image: ghcr.io/szl-holdings/rosie:###ZARF_PKG_TMPL_VERSION###
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 7860
              protocol: TCP
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: rosie
  namespace: szl-rosie
  labels:
    app: rosie
    app.kubernetes.io/name: rosie
spec:
  selector:
    app: rosie
  ports:
    - name: http
      port: 7860
      targetPort: 7860
      protocol: TCP
