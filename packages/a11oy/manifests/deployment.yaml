# Copyright 2026 SZL Holdings
# SPDX-License-Identifier: Apache-2.0
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: a11oy
  namespace: szl-a11oy
  labels:
    app: a11oy
    app.kubernetes.io/name: a11oy
    szl.io/component: uds-workload
spec:
  replicas: 1
  selector:
    matchLabels:
      app: a11oy
  template:
    metadata:
      labels:
        app: a11oy
        app.kubernetes.io/name: a11oy
    spec:
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        runAsGroup: 65532
        fsGroup: 65532
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: a11oy
          # Live, cosign-signed image. Zarf rewrites this to the in-cluster
          # registry reference at deploy time.
          image: ghcr.io/szl-holdings/a11oy:###ZARF_PKG_TMPL_VERSION###
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: a11oy
  namespace: szl-a11oy
  labels:
    app: a11oy
    app.kubernetes.io/name: a11oy
spec:
  selector:
    app: a11oy
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
