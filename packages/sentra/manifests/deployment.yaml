# Copyright 2026 SZL Holdings
# SPDX-License-Identifier: Apache-2.0
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sentra
  namespace: szl-sentra
  labels:
    app: sentra
    app.kubernetes.io/name: sentra
    szl.io/component: uds-workload
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sentra
  template:
    metadata:
      labels:
        app: sentra
        app.kubernetes.io/name: sentra
    spec:
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        runAsGroup: 65532
        fsGroup: 65532
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: sentra
          # Live, cosign-signed image. Zarf rewrites this to the in-cluster
          # registry reference at deploy time.
          image: ghcr.io/szl-holdings/sentra:uds-v0.2.0@sha256:60a0efc14366ba392bfe3f3cd4196863fe148bb87a17428be6a57f0a05ac3639
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: sentra
  namespace: szl-sentra
  labels:
    app: sentra
    app.kubernetes.io/name: sentra
spec:
  selector:
    app: sentra
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
