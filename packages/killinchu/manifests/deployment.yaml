# Copyright 2026 SZL Holdings
# SPDX-License-Identifier: Apache-2.0
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: killinchu
  namespace: szl-killinchu
  labels:
    app: killinchu
    app.kubernetes.io/name: killinchu
    szl.io/component: uds-workload
spec:
  replicas: 1
  selector:
    matchLabels:
      app: killinchu
  template:
    metadata:
      labels:
        app: killinchu
        app.kubernetes.io/name: killinchu
    spec:
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        runAsGroup: 65532
        fsGroup: 65532
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: killinchu
          # Live, cosign-signed image. Zarf rewrites this to the in-cluster
          # registry reference at deploy time.
          image: ghcr.io/szl-holdings/killinchu:uds-v0.2.0@sha256:f6fd13ed28c65a195fd96369861adb2bba9ec2d1717ea494442fc0a79e5d6f85
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 7860
              protocol: TCP
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          env:
            # serve.py listens on $PORT (default 7860). Pin it so the
            # containerPort / Service targetPort below match the real listener.
            - name: PORT
              value: "7860"
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: killinchu
  namespace: szl-killinchu
  labels:
    app: killinchu
    app.kubernetes.io/name: killinchu
spec:
  selector:
    app: killinchu
  ports:
    - name: http
      port: 7860
      targetPort: 7860
      protocol: TCP
