# Copyright 2026 SZL Holdings
# SPDX-License-Identifier: Apache-2.0
#
# uds-mesh Helm chart — cross-component governance-receipt span relay.
# Vendored locally so uds/zarf.yaml's `uds-mesh` component is fully checked by
# deploy-entry-guard (namespace agreement + wait-target reality) rather than
# reported UNVERIFIABLE. The deployed reality (Deployment `uds-mesh-relay` in the
# `szl-vessels` namespace) matches the component's onDeploy readiness wait.
apiVersion: v2
name: uds-mesh
description: "SZL uds-mesh — cross-component governance-receipt span relay to vsp-otel"
type: application
version: 0.1.0
appVersion: "0.1.0"
home: https://github.com/szl-holdings/uds-mesh
sources:
  - https://github.com/szl-holdings/uds-mesh
keywords: [uds, szl, mesh, otel, spans]
maintainers:
  - name: SZL Holdings
    email: eng@szlholdings.com
