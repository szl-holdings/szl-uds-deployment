# Copyright 2026 SZL Holdings — SPDX-License-Identifier: Apache-2.0
# AMBIENT mesh: NO sidecar.istio.io/inject annotation (forbidden by the
# pepr-uds-core webhook in ambient mode). The container listens on
# .Values.service.targetPort.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: killinchu
  namespace: {{ .Values.namespace }}
  labels:
    app: killinchu
    app.kubernetes.io/name: killinchu
    app.kubernetes.io/part-of: szl-uds-mesh
spec:
  replicas: {{ .Values.replicas }}
  selector:
    matchLabels:
      app: killinchu
  template:
    metadata:
      labels:
        app: killinchu
    spec:
      securityContext:
        runAsNonRoot: {{ .Values.securityContext.runAsNonRoot }}
        runAsUser: {{ .Values.securityContext.runAsUser }}
        seccompProfile:
          type: {{ .Values.securityContext.seccompProfile.type }}
      containers:
        - name: killinchu
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.service.targetPort }}
              name: http
          envFrom:
            - configMapRef:
                name: killinchu-doctrine
          volumeMounts:
            - name: receipt-key
              mountPath: /etc/szl/receipt-key
              readOnly: true
          securityContext:
            allowPrivilegeEscalation: {{ .Values.securityContext.allowPrivilegeEscalation }}
            readOnlyRootFilesystem: {{ .Values.securityContext.readOnlyRootFilesystem }}
            capabilities:
              drop: {{ .Values.securityContext.capabilities.drop | toYaml | nindent 16 }}
          readinessProbe:
            httpGet:
              path: {{ .Values.healthPath }}
              port: {{ .Values.service.targetPort }}
            initialDelaySeconds: 5
            periodSeconds: 10
          resources: {{ .Values.resources | toYaml | nindent 12 }}
      volumes:
        - name: receipt-key
          secret:
            secretName: {{ .Values.receiptKeySecret }}
            optional: true
