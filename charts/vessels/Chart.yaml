# Copyright 2026 SZL Holdings
# SPDX-License-Identifier: Apache-2.0
#
# vessels Helm chart — SZL maritime vessel intelligence demo service.
# Vendored locally so uds/zarf.yaml's `vessels-web` component is fully checked
# by deploy-entry-guard (namespace agreement + wait-target reality), not merely
# reported UNVERIFIABLE. The deployed reality (Deployment `vessels-web` in the
# `szl-vessels` namespace) matches the component's onDeploy readiness wait.
apiVersion: v2
name: vessels
description: "SZL maritime vessel intelligence — sanctions screening, dark-vessel detection, ownership graph, voyage analytics"
type: application
version: 0.3.1
appVersion: "0.3.1"
home: https://github.com/szl-holdings/vessels
sources:
  - https://github.com/szl-holdings/vessels
keywords: [uds, szl, vessels, maritime, sanctions]
maintainers:
  - name: SZL Holdings
    email: eng@szlholdings.com
