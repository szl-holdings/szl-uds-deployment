{
  "name": "szl-receipts-tests",
  "version": "0.1.0",
  "description": "Playwright journey tests for szl-receipts UDS package",
  "scripts": {
    "test": "playwright test",
    "test:list": "playwright test --list"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0"
  }
}
